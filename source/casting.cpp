// #include <bits/stdc++.h>
#include <iostream>   
#include <vector>     
#include <algorithm>  // For algorithms like std::sort

#define rep(a, b)   for(int a = 0; a < (b); ++a)
#define all(a)      (a).begin(),(a).end()
// #define endl        '\n'

using namespace std;
using Graph = vector<vector<int>>;
using ll = long long;

vector<int> transmutate(string colors){
    vector<int> indices;
    cout << "YES" << endl;
    return indices;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.precision(10);

    int t; cin>>t;
    string colors;
    while (t--) {
        cin >> colors; 
        char target = colors[0];
        cout << colors << endl;
        cout << target << endl;
    }

    return 0;
}



